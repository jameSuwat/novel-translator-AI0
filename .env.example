# Copy this file to .env.local for local development (only read by the /api/gemini
# serverless function when run via `vercel dev`).
# In production, set this as an Environment Variable in your Vercel project settings instead.
GEMINI_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
